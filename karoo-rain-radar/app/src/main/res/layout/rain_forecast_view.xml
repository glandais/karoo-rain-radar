<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- Circular rain visualization will be drawn as a bitmap -->
    <ImageView
        android:id="@+id/rainCircleImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:contentDescription="Rain forecast circle" />

    <!-- Center dot representing current position -->
    <View
        android:id="@+id/centerDot"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:layout_gravity="center"
        android:background="@android:color/black" />

    <!-- Status text at bottom -->
    <TextView
        android:id="@+id/rainStatusText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:textSize="10sp"
        android:textColor="@android:color/white"
        android:background="#80000000"
        android:padding="2dp"
        android:text="" />

</FrameLayout>
